// 2477. [모의 SW 역량테스트] 차량 정비소
#include <iostream>
#include <algorithm>
#include <queue>
#include <vector>
using namespace std;
int N, M, K, A, B;
int a[10], b[10]; // 창구
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int tc;
    cin >> tc;
    for (int ts = 1; ts <= tc; ts++)
    {
        // 접수창구 갯수, 정비 창구 갯수, 방문 고객 수, 접수 창구 효율, 정비 창구 정비 효율
        cin >> N >> M >> K >> A >> B;
    }
    return 0;
}